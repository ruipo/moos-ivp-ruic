%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%% LOG FILE:       ./LOG_SHORESIDE_16_3_2020_____22_34_41/LOG_SHORESIDE_16_3_2020_____22_34_41._moos
%% FILE OPENED ON  Wed Dec 31 19:00:00 1969
%% LOGSTART               1584412480.46
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
//*******************************************************
// ShoreSide Configuration File

ServerHost = localhost
ServerPort = 9000
Community  = shoreside

MOOSTimeWarp = 1
LatOrigin  = 43.825300 
LongOrigin = -70.330400 


  
//--------------------------------------------------------
Processconfig = ANTLER
{
  MSBetweenLaunches = 100

  Run = MOOSDB          @ NewConsole = false
  Run = uXMS      @ NewConsole = true
  Run = pMarineViewer   @ NewConsole = true
  Run = pPointAssign    @ NewConsole = true
  Run = pLogger         @ NewConsole = false
  Run = pShare          @ NewConsole = false
  Run = pHostInfo       @ NewConsole = false
  Run = uFldShoreBroker @ NewConsole = false
  Run = uTimerScript    @ NewConsole = true
  //Run = pGenPath        @ NewConsole = true

}

//---------------------------------------------------------
// pShare Config Block  (Created from Plugin)

ProcessConfig = pShare
{
  AppTick    = 2
  CommsTick  = 2

  input = route =  localhost:9300
}


//--------------------------------------------------
// pHostInfo configuration block from plugin

ProcessConfig = pHostInfo
{
  AppTick 	= 1
  CommsTick 	= 1

  DEFAULT_HOSTIP_FORCE = localhost
}


//--------------------------------------------------
// pLogger Configuration Block (Created from Plugin)

ProcessConfig = pLogger
{
  AppTick 	= 10
  CommsTick 	= 10
  
  File		= LOG_SHORESIDE
  PATH		= ./
  AsyncLog 	= true
  FileTimeStamp = true

  // Log it all!!!!!
  LogAuxSrc       = true
  WildCardLogging = true 

  WildCardOmitPattern = *_STATUS

  CompressAlogs = false
}


//------------------------------------------
// uXMS configuration  block

ProcessConfig = uXMS
 {
   VAR            = VISIT_POINT,VISIT_POINT_HENRY, VISIT_POINT_GILDA, UPDATES_V1, UPDATES_V2
   HISTORY_VAR    = VISIT_POINT_OUT
 }

//--------------------------------------------------
// uFldShoreBroker Configuration Block

ProcessConfig = uFldShoreBroker
{
  AppTick       = 1
  CommsTick     = 1

  QBRIDGE  = DEPLOY, NODE_REPORT
  QBRIDGE  = STATION_KEEP, APPCAST_REQ
  QBRIDGE  = MOOS_MANUAL_OVERRIDE

  BRIDGE   = src=VISIT_POINT_ALL, alias=VISTI_POINT
  BRIDGE   = src=VISIT_POINT_$V,  alias=VISIT_POINT

  BRIDGE   = src=START_TIMER_ALL, alias=START_TIMER
  BRIDGE   = src=START_TIMER_$V,  alias=START_TIMER

  BRIDGE   = src=REFUEL_NEEDED_ALL, alias=REFUEL_NEEDED
  BRIDGE   = src=REFUEL_NEEDED_$V,  alias=REFUEL_NEEDED

  BRIDGE   = src=RETURN_ALL, alias=RETURN
  BRIDGE   = src=RETURN_$V,  alias=RETURN
}

//--------------------------------------------------------
ProcessConfig = pMarineViewer
{
  AppTick    = 4
  CommsTick  = 4

  TIFF_FILE            = forrest19.tif
  set_pan_x            = -90
  set_pan_y            = -280
  zoom                 = 0.65
  vehicles_shape_scale = 1.5
  vehicles_name_mode   = names+shortmode

  point_viewable_labels   = true
  polygon_viewable_labels = false
  appcast_viewable     = true
  appcast_color_scheme = indigo
  hash_delta           = 50
  hash_viewable        = true
  hash_shade           = 0.35

  SCOPE = STATION_KEEP
  SCOPE = REFUEL_NEEDED

  BUTTON_ONE   = DEPLOY  # DEPLOY_ALL=true 
  BUTTON_ONE   = MOOS_MANUAL_OVERRIDE_ALL=false 
  BUTTON_ONE   = RETURN_ALL=false # STATION_KEEP_ALL=false # REFUEL_NEEDED_ALL=false # START_FTIMER_ALL=true

  BUTTON_TWO   = RETURN  # RETURN_ALL=true
  BUTTON_TWO   = STATION_KEEP_ALL=false # REFUEL_NEEDED_ALL=false

  BUTTON_THREE = STATION  # STATION_KEEP_ALL=true # REFUEL_NEEDED_ALL=false # RETURN_ALL=false #START_TIMER_ALL=false
  
  BUTTON_FOUR = REFUEL # REFUEL_NEEDED_ALL=true
  BUTTON_FOUR = STATION_KEEP_ALL=false # RETURN_ALL=false

}

 //------------------------------------------
 ProcessConfig = uTimerScript                                    
 {                            
   paused=true
   pause_var=UTS_PAUSE

   rand_var    = varname=XRND_VAL, min=-25, max=200, key=at_post
   rand_var    = varname=YRND_VAL, min=-175, max=-25, key=at_post

   event = var=VISIT_POINT, val="firstpoint", time=0
   event = var=VISIT_POINT, val="x=$[XRND_VAL], y=$[YRND_VAL], id=$[TCOUNT]", time=5, amt=1
   event = var=VISIT_POINT, val="x=$[XRND_VAL], y=$[YRND_VAL], id=$[TCOUNT]", time=6, amt=1  
   event = var=VISIT_POINT, val="x=$[XRND_VAL], y=$[YRND_VAL], id=$[TCOUNT]", time=7, amt=1
   event = var=VISIT_POINT, val="x=$[XRND_VAL], y=$[YRND_VAL], id=$[TCOUNT]", time=8, amt=1 
   event = var=VISIT_POINT, val="x=$[XRND_VAL], y=$[YRND_VAL], id=$[TCOUNT]", time=9, amt=1
   event = var=VISIT_POINT, val="x=$[XRND_VAL], y=$[YRND_VAL], id=$[TCOUNT]", time=10, amt=1 
   event = var=VISIT_POINT, val="x=$[XRND_VAL], y=$[YRND_VAL], id=$[TCOUNT]", time=11, amt=1
   event = var=VISIT_POINT, val="x=$[XRND_VAL], y=$[YRND_VAL], id=$[TCOUNT]", time=12, amt=1 
   event = var=VISIT_POINT, val="lastpoint", time=15
 } 

//------------------------------------------
 // pPointAssign config block

 ProcessConfig = pPointAssign                                    
 {                            

   //AppTick   = 4
   //CommsTick = 4

   VNAME=HENRY
   VNAME=GILDA
   ASSIGN_BY_REGION=false
 } 

//------------------------------------------
 // pGenPath config block

 ProcessConfig = pGenPath                                    
 {                            

   //AppTick   = 4
   //CommsTick = 4

   VNAME=HENRY
   VNAME=GILDA
 } 

